{
  "name": "dy-sobang-fieldlog",
  "version": "1.0.0",
  "description": "대영소방 사진기록 - 현장 사진+메모+GPS 자동 기록 앱",
  "private": true,
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "android:release": "npx cap sync android && cd android && ./gradlew assembleRelease",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app-launcher": "^6.0.2",
    "@capacitor/browser": "^6.0.3",
    "@capacitor/core": "^6.1.2",
    "@capacitor/filesystem": "^6.0.1",
    "@capacitor/geolocation": "^6.0.1",
    "@capacitor/share": "^6.0.2",
    "@capacitor/status-bar": "^6.0.2",
    "@capacitor-community/speech-recognition": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
